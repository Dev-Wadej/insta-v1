/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["www.pngitem.com", "upload.wikimedia.org"]

    }
}

module.exports = nextConfig